import React, { Component } from 'react';

const contact = {
  background: '#999',
  height: '400px'
}

class Contact extends Component {
  render() {
    return (
        <div css={contact}>
        </div>
    );
  }
}

export default Contact;
